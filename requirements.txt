ultralytics        # pip install ultralytics opencv-python-headless

# picamera2         # sudo apt install -y python3-picamera2

# If using a venv, create it so it can see the system picamera2:
#   python3 -m venv --system-site-packages .venv
